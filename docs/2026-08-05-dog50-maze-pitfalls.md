# 机器狗 50m 迷宫导航 — 踩坑记录（2026-07-11 ~ 08-05）

> 会话易失，文档长存。所有坑按「现象 → 根因 → 修复」记录，供后续会话快速恢复上下文。
> 详细分析见 `~/.hermes/skills/robotics/robot-navigation-algorithms/references/` 下的 2026-07-12 系列文档。

## 一、核心设计原则（主人指令，不可违背）

| 原则 | 内容 |
|---|---|
| **碰撞=0** | 现实中不允许碰撞。不靠物理碰撞挡（slide直驱不可靠会穿墙），靠**运动学约束**：前瞻测距 + 制动约束 v≤sqrt(2·A_DECEL·d) + 加速度限制 + 预判转向 |
| **不作弊** | 真实地图未知，从头扫描。A* 必须用 SLAM grid，**禁止用世界真值规划**。UNKNOWN 可通行但代价高（traversable + UNKNOWN_PENALTY=8），执行层撞墙写回 WALL |
| **效率** | 50m 小地图不该走几十万步。参考成熟 frontier_exploration：门聚类/评分/换门/去噪 |

## 二、卡死三连环 bug（墙边场景）

1. **scan 墙前膨胀**：`gset(prev, WALL)` 把机器人脚下格标 WALL → find_gates 起点失败 → 空门。修复：墙前格标 FREE。
2. **find_gates 起点过严**：贴墙时 walkable=False → 空门 → 原地 bounce 死循环。修复：起点放宽 `gget!=WALL` + `_nearest_walkable` BFS 脱困。
3. **主循环 path=None 只 _bounce 不 step**：永不移动，stuck 检测不触发。修复：path=None 调 `mv.step(FINISH)`。

## 三、迷宫赛道结构（关键！）

50m 蛇形赛道**不是简单蛇形走廊，是迷宫**：
- 10 段水平走廊（y=2.5+5k），段间墙**有缺口**（y=15.0 缺口在 x<4.1）
- 连接段（x≈2.5/47.5）有横向段间墙，缺口窄（~10° 视角，如右侧 y=9.3-10.6 缺口在 x≈46）
- **`_bounce` 候选必须每 5° 扫 360° + 推进评分**——稀疏角度（±[10,20,35,...]°）漏掉窄缺口是卡死主因

## 四、探索效率 8 项改进（commit 1a03d58）

1. 门聚类质心 `cluster_gates()`：200 单格 → 4-10 region
2. `pick_gate` score 模式：`0.55·advance + 0.25·(1/dist) + 0.20·(size/50)`（终点导向）
3. `traversable()`：UNKNOWN 可通行 + UNKNOWN_PENALTY=8
4. 失败换门：A* 失败试下 3 个
5. `back_blacklist`：BACK 过没新门的路标跳过
6. 碰撞检测用真实几何 `is_obstacle_world`（0.7m）非 blocked()（0.9m 假阳性）
7. Mover 硬防穿墙 + 撞墙写回 WALL
8. `_bounce` 每 5° 扫 + 推进评分（`dot·2 + min(d,4)`）

## 五、最终成绩

| seed | arrived | cov | collision | steps | time |
|---|---|---|---|---|---|
| 7 | ✅ | 99.89% | **0** | 213502 | 728s |
| 99 | ⏳差8m | 81.4% | **0** | 232210 | 800s超时 |
| 170456 | ✅ | 99.87% | **0** | 239464 | 768s |

2/3 到达终点，全部碰撞=0。800s/500m ≈ 0.6m/s，主人认为可接受——**稳定优先，再考虑优化**。

## 六、提速实验（2026-08-05，4m/s）

主人问"最大速度提到4m/s有用吗"。实测结论：**有用但效果有限**（瓶颈在 bounce/探索不在直线速度）。

| seed | 2m/s 版 | 4m/s 版 | 变化 |
|---|---|---|---|
| 7 | ✅ 728s / 21.3万步 | ✅ 649s / 18.3万步 | -12% 时间，-14% 步数 |
| 170456 | ✅ 768s / 23.9万步 | ⏳ 700s 差3m | 接近持平（bounce 221→252） |

**关键组合改动**（commit 4c34c8b）：
1. SPEED_MAX 2→4, A_ACCEL 3→5, A_DECEL 5→8
2. **YAW_RATE 保持 1.5 不提高**——第一次试 2.5+4m/s 窄缺口过冲卡死（7万步卡连接段）
3. **近墙限速**：`d_clear < 2.0m 时 v_des ≤ 1.5`——开阔跑 4m/s，狭窄处自动减速防过冲

制动距离验证：v=4, A_DECEL=8 → v²/2a = 1.0m < LOOKAHEAD 4.0m ✅ 安全。

**教训：光提速度没用，必须配合"近墙限速"**——真实机器人行为：开阔提速、狭窄减速。

## 七、待办

- seed 99 / 170456 到达（差 8m/3m 超时，加时即可）
- 步数优化方向：减少 bounce（VO 局部避障）、降低扫描密度、并行探索
- 真狗平台迁移

## 八、双层地图 + 射线清除（2026-08-05 10小时目标）

主人 10 小时目标：①探索效率 ②已知地图+障碍变化快速寻路（走中间）③随机位置绑架恢复。

### 性能元凶（重大发现）
- **EGL 离屏渲染 3.5 秒/帧**（llvmpipe 软件渲染，与分辨率无关）——视觉识别每 40 步渲染一帧会把导航从 14.9万步/s 拖到 ~10步/s！
- **解决**：视觉默认关闭（`--vision 1` 才开），标牌几何默认关闭（`--landmarks 1` 才开）
- 教训：**渲染类功能必须默认关**，否则拖死核心算法

### 双层地图（借鉴 ROS costmap 分层）
- `static_grid`：旧地图背景（只读），`KNOWN_MAP_MODE` 时规划叠加
- `gget_plan()`：规划视图 = static 墙永远 WALL + live 障碍/自由优先
- `scan()` 加**射线清除**：射线穿过格强制 FREE（旧障碍被"照"掉），命中格 WALL
- 解决"已知地图+障碍变化"：旧障碍位置新扫描自动清除，static 的墙不受影响

### 走中间 Voronoi 代价
- `VORONOI_C=2.0`：KNOWN_MAP_MODE 下 A* 代价 + C/d²（d=离墙格数）
- 贴墙爆炸、中线最低——路径自然贴通道中线

### 新参数
- `--save-map <npz>` 阶段1保存地图 / `--load-map <npz>` 阶段2加载
- `--random-start 1` 绑架随机起点（避开墙/障碍） / `--target start|finish`
- `--obs-reseed <step>` B阶段运行中障碍变化
- `--vision 1` / `--landmarks 1` 视觉/标牌（默认关）

### 标牌贴墙（主人"别堵路"）
- 标牌贴**段尾横墙内壁**（x=49/1 交替），机器人沿 x 正对可见
- 侧墙标牌不可见（相机朝前看不到 y 方向墙）——正前方横墙才可见

## 九、08-06 二维码标牌攻坚（hfield 世界修正）

主人方向：标准黑白二维码（DICT_7X7 四角明显）+ 图像金字塔多尺度 + 标牌放大 2 倍 + 贴墙 1m 高。

### 9.1 世界模型（最重要发现）
- hfield `size="25 25 4 2"` → 表面高度 = 2 + 4×(灰度/255)。**通道(128)=4.008m，墙(191)=4.996m**
- **机器狗 body 必须在 hfield 表面上方**（BOT_Z=HF_SURF+0.5）——否则相机 z=0.5 在地底下，**全黑**！多轮"标牌不渲染/全暗"的元凶
- 标牌必须贴 hfield 表面（z=HF_SURF+0.02），地面 z=0.02 被实体 hfield 埋住

### 9.2 相机 roll 修正
- `euler="0 -1.5708 0"` 朝前但 **right=+z（天空在水平方向）→ 画面 roll 90°**（地面在左、天空在右，主人截图发现）
- MuJoCo euler 是 **extrinsic xyz**（先 z 后 y 后 x 作用）——绕 y -90° 后要 up=+z 需加 z=-90°
- 修正：`euler="0 -1.5708 -1.5708"` → up=+z（天空上）、right=-y、fwd=+x ✅
- 验证法：红/黄/绿/蓝球放相机前后左右，看图像位置（绿球 -y 应在右侧）

### 9.3 plane 纹理旋转方向
- **绕 y 旋转（横墙）纹理可靠，绕 x 旋转（侧墙）纹理异常**——侧墙标牌所有距离/所有翻转方向都识别失败
- 结论：标牌只能贴横墙（法线 ±x）

### 9.4 ArUco 需要闭合背景
- 标牌在转弯口开口区（无墙背景）→ ArUco 边界分离失败
- 修复：标牌后面 0.6m 加**深色背景板**（2.6m box，比标牌大 0.3m/边）——像真实路牌
- **背景板方向 bug**：写反了挡在标牌前面 → 修正 side==1 背景在 +x

### 9.5 标牌尺寸与位置
- 1.5m 标牌在狗 2m 距离太大占满视野（ArUco 失败）；0.75m 配合金字塔 scale=0.5 稳定
- 主人指令放大 2 倍 → **2m×2m**（size 半尺寸 1.0）
- 标牌中心必须**通道中心线**（y=2.5+ch*5）——贴分界墙边缘会嵌进墙（码只显示一半）

### 9.6 金字塔级联粗筛（主人思路）
- 每帧 6 档金字塔全跑（无标牌 25ms 浪费）→ **0.25x Laplacian 能量 <200 跳过全部**
- 实测判别：无标牌 23-84，有标牌 231-1684，阈值 200 零重叠；粗筛 0.3ms
- 精识别用 INTER_LINEAR（INTER_AREA 缩小模糊小码致检测失败）

### 9.7 box 墙堵死 U 型弯（主人一眼看出）
- 现象：侧墙 x∈[0,50] 全程 + 横墙 x=0.25/49.75 → **所有 U 型弯堵死**
- 迷宫真实结构（实测 track_clean）：分界墙 y=5k，**转弯口交替开口**（y=5,15,25,35,45 右开口 x=50；y=10,20,30,40 左开口 x=0）
- 修复：分界墙只到开口前（奇数右墙 x∈[0,45.5]、偶数左墙 x∈[4.5,50]），删横墙
- **教训：加几何前先验证地图真实结构**（先跑连通性/看墙段，再改场景）

## 十、08-06 探索效率三个根因修复

主人问性能后死磕，找到 3 个根因（systematic-debugging）：

### 10.1 find_gates 0 门 bug
- 现象：狗反复 bounce 卡死，gates=0
- 根因：`cluster_gates(gates, min_size=8)`——探索早期前沿窄（门格 3-5 个 <8）→ **全被过滤 → 0 门**
- 独立复现：3 门格 → []，5 门格 → []，12 门格 → 正常
- 修复：min_size 动态化（min(8, max(2, len//2))）+ 全过滤时保底返回最近门

### 10.2 薄斜墙漏扫（最大发现）
- 现象：狗在 (48.5,2.2) 被挡，wall=False obs=False 但 d_clear=0.35
- 排查：track_clean 有 **45° 薄斜墙**（0.1m 厚）——bounce 点前方 0.35m 正好撞上
- 根因链：LIDAR_RAYS=120（3°）→ 15m 处射线间距 0.78m > 墙厚 → 漏扫 → 斜墙格=UNKNOWN → A* 认为可走 → 执行层 blocked 撞墙
- 修复：LIDAR_RAYS 120→360（1°）；LIDAR_TICK 20→10（减少两次 scan 间盲区）
- 效果：coverage 13%→31%（2.4×），狗从卡通道 0 推进到通道 2

### 10.3 ASTAR_MAX_EXPAND 不足
- 现象：KNOWN_MAP_MODE 每步 bounce 原地不动
- 根因：蛇形迷宫全程 A* 需扩展 >3 万格（BFS 全图 23.5 万格），ASTAR_MAX_EXPAND=30000 不够 → astar_to 返回 None
- 修复：30000→250000 → A* 成功规划 451m 全程（522 格）

## 十一、迷宫连通性 & 性能基线（08-06 晚）

- **476m 蛇形迷宫完全连通**：起点 (2.5,2.5)→终点 (2.5,47.5) 无墙阻断；BFS 23.5 万格全通，10 通道中心 49/49
- 障碍固定（seed7=10 个，半径 0.5m，不移动；--obs-reseed 运行中换）
- 性能：KNOWN_MAP 540s 到通道 5（55%，bounce 73）；探索 540s 覆盖 52%（bounce 58）；全程估算 ~900-1000s
- 速度 ~120 步/s（理论 200），瓶颈 bounce（斜墙）+ 近墙限速

## 十二、待办（2026-08-06 状态）

- [ ] 探索效率：500s 内完成（当前 540s 52%）——斜墙平滑避让是主攻
- [ ] KNOWN_MAP 全程验证（~900s 估算，未实测走完）
- [ ] 绑架恢复完整验收（--random-start + --load-map + --target）
- [ ] B 阶段 --obs-reseed 验证
- [ ] 标牌与导航联动（看到标牌修正位姿，当前只记录）
- [ ] 文档：README.md 已建，mystory 机器人仿真 story.md 待更新

## 十三、08-07 里程碑：HPA* + yaw bug 修复（终极三坑）

### 13.1 全程 A* 50.8s — 移植 HPA* 分层寻路
- 现象：KNOWN_MAP 全程规划 50.8s（缓存空），分段 A* 每段仍 430-560ms 超 100ms 铁律
- 根因：全程 A* 需扩展数万格，wall_dist 每格扫 441 次 × 25 万格 = 1.1 亿查询
- 修复：移植 ROS 标准 HPA*（`scripts/hpa_star.py`）——CELL=50 格(5m)、216 门、cell 内 BFS 配对，粗层门网络+细层短 A*。**全程规划 0.38ms**（13 万倍）
- 教训：主人方法论"先移植成熟算法，不自研死磕"——ros 就在那放着

### 13.2 距离场 BFS 取 min bug — 贴墙路径根源
- 现象：HPA 反向路径贴墙走 x=45.05（物理墙 44.975），狗 bounce 卡死 (45.2,34.9)
- 根因：`di[j] = n if n < v else (w if w < v else v)`——**n 优先，忽略更小的 w**。墙多时 n 已传播（50），左邻是墙（1），错误取 50 → 距离场以为开阔 → 路径贴墙
- 独立测试墙稀疏时 n=INF+1>v 走 w 分支碰巧正确——单测过了不代表真地图对
- 修复：`if w < n and w < v: di[j] = w`（取三值最小）→ dist[450]=1（正确）→ 路径走开口中间
- 教训：BFS 传播必须取 min(v,n,w)，不能"n 优先"

### 13.3 yaw 控制被物理读回覆盖 — 终极根因 🎯
- 现象：绑架恢复 seed42 finish 150s 卡死 bounce 120+，狗直线冲出通道往 x=48 跑；DBG 显示 yaw 恒 0° 不转
- 根因：`self.d.qvel[2]=0; mujoco.mj_step(...); self.yaw = self.d.qpos[2]`——**每步转向 dyaw 后被物理 qpos[2]（恒 0）重置**。狗从未真正转向，永远朝初始方向直冲
- 为什么之前 KNOWN_MAP 能跑通：起点 (2.5,2.5) yaw=0 恰好朝路径方向，靠 bounce+限速硬走完（bounce 94）
- 修复：yaw 是控制变量，**直接写回物理不读回**（`d.qpos[2]=self.yaw`；滑动模型 friction=0 mj_step 保持）
- 效果：bounce 94→0，KNOWN_MAP 241s→125s，探索 242s→127s，绑架 4/4 全到达
- 教训：**控制变量和物理状态分离时，读回方向要三思**——`self.yaw = d.qpos[2]` 这种"同步"会吃掉所有控制量

### 13.4 大转向限速（DWA 思想）
- 现象：修复 yaw 后仍需防直冲——随机起点 yaw=0 但目标在背后 170°，转向慢会先冲一段
- 修复：`abs(err)>1.0 rad` 时限速 1.0m/s，先转身再加速（曲率越大速度越低）
- 效果：配合 yaw 修复，bounce 全 0

### 13.5 任意通道目标 --target ch<N>
- 现象：--target 只支持 start|finish，无法导航到任意通道二维码
- 修复：解析 landmark_positions()（每通道 1 标牌），`--target ch4` → 目标 (45.8,22.5)
- 验证：ch0-9 全通道 100% 到达 bounce 0

### 13.6 ARRIVE 判定 3.0 → 3.5m
- 现象：ch0 目标 (45.8,2.5) 狗停在 (46.0,5.5)，距离 3.007m 差 7mm 判不到
- 根因：二维码在通道尽头转弯口前，3.0m 在斜墙边会差一点
- 修复：3.5m（够近即可）

## 十四、M2 障碍渐进六坑（08-07）

### 14.1 写圈写 live grid 会被 SLAM 射线清除擦掉
- 现象：障碍安全圈写进 grid 后反复被擦，狗反复撞同一障碍（写圈日志重复 10+ 次）
- 根因：`scan()` 的"射线清除"——穿过格强制标 FREE（清旧障碍），把安全圈 WALL 当"旧障碍"擦掉
- 修复：**安全圈写 static_grid（永久层），不写 live grid**。HPA 的 `gget_plan` 叠加 static+live，都能看到

### 14.2 escape 走太远（400 步=8m）撞别的墙
- 现象：escape 沿"远离障碍"方向走 8m，撞上另一面分界墙 → 又 bounce → 死循环
- 根因：escape 只需绕出障碍安全圈（2-3m），走 8m 会撞别的墙
- 修复：escape_steps 400→150（0.75s ~2m）

### 14.3 escape 方向"朝目标"评分选撞墙方向
- 现象：卡点 (9.4,17.8) 全向扫描 `dot*4+min(d,2)` 选 90°（朝 y=20 墙 d=2.0m，score 5.5）> 路径方向 150°（绕障碍 d=0.7m，4.7）→ 狗撞墙死循环
- 根因：评分以"朝目标"为主，d 只截断 2——撞墙方向 d 大反而得分高
- 修复：escape 方向 = **全向扫描可走距离最大**（不朝目标）。撞墙方向 d_clear 短天然被淘汰

### 14.4 动态 blocked 死锁（速度塞进碰撞判定）
- 现象：`dyn_clear = 0.7 + v²/2a` 后，狗离障碍 1.2m 加速到 3m/s，安全距离 1.26m > 1.2m → 第一点 blocked → d_clear=0 → v_brake=0 → 永远无法加速
- 根因：碰撞判定混入速度——高速时"当前位置"就被判 blocked，狗被困死
- 修复：**碰撞判定固定物理边界 0.7m**；速度自适应由 v_brake 制动约束实现（接近障碍 d_clear 短 → 限速 → 低速挤窄缝）

### 14.5 5×5 邻域判定过保守（+0.283m）
- 现象：窄缝（两障碍中心距 1.4m）挤不过，狗被挡在障碍 0.98m 外
- 根因：blocked 检查 5×5 邻域格中心，邻域格中心距狗中心最远 √(2²+2²)×0.1=0.283m → 等效安全距离 0.7+0.283
- 修复：**精确圆判定**——狗中心距障碍 <0.7m 直接判定，无格离散冗余

### 14.6 HPA 门网络静态（写圈后 plan None）
- 现象：写圈后 HPA plan 返回 None（0 格），狗卡死 bounce 299
- 根因：`HPAStar.__init__` 构建门网络/距离场时用当时 static 快照——写圈后门可能被障碍圈堵住，但粗层门网络不知道 → nearest_gate 返回被堵门 → 细层失败
- 修复：**写圈后重建 HPA**（1.7s）。注意：0.8m 圈 + 精确圆判定后重建可行；1.0m 圈 + 5×5 判定时重建会把缝堵死（bounce 1741）——三个修复必须配套

## 十五、08-09/08-10 无特权化 + 门宽度判断 九坑（本轮实测定位）

### 15.1 跳步粒度奇偶 —— 直奔 beeline 永远落不上目标格 🎯
- 现象：狗看到终点、三角收敛误差 0.02m、物理走到距球 0.17m，却卡在"0门→BACK→A* 风暴"
  （301 次全程 A*，速率 163→37 步/s）直到超时
- 根因：`jump_steps` 开放区跳步恒取上限（2m），目标格不在跳步格点上永远落不上去
  → `astar_to` 每次扩满 25 万格返回 None → 直奔瘫痪。开阔通道里目标格可达性≈奇偶运气
- 修复：**目标对齐吸附**——走查跨过目标行/列时收步落在目标坐标上（先落行/列，再落到格）。
  教训：基线没踩中纯属"门还没探完"的运气——**回归时机的微小变化就能暴露预埋 bug**

### 15.2 障碍视觉冻结 —— MuJoCo 无关节 body 位置从不更新
- 现象：录像里 10 个移动障碍全冻在初始位置（一列齐排 x=25），"看不到在移动"
- 根因：障碍是无关节静态 body（`contype=0`），`xpos` 不随 obs_world 更新——
  **逻辑上一直在动**（雷达/碰撞/DWA 都感知真位置，回归成绩是真的），只是渲染 frozen
- 修复：障碍 body 全部 `mocap="true"`，每 tick `sync_obs_mocap()` 同步位置。
  教训：**仿真"逻辑真"≠"渲染真"**，视觉验证（主人看录像）独立钓出 bug

### 15.3 接触区豁免擦掉固定障碍 —— 狗开进障碍体内卡 7700 步 🎯
- 现象：混合场 collision 8127，狗在 5 号弯障碍体内（0.26m）冻 7700 步
- 根因：接触区豁免（防移动障碍把狗逼死的机制：障碍命中格距狗 <0.5m 标 FREE）
  对 obs_world **全部**生效——混合场狗贴近**固定**弯道障碍时，固定障碍也被从地图上
  擦掉 → 狗对眼前的障碍失明
- 修复：豁免仅限移动障碍（`_moving_obs_positions()`），固定障碍贴近保持 WALL
  （有安全圈+刹车兜底）。教训：**为动态场景设计的豁免机制，作用域必须区分动/静**

### 15.4 弯道障碍摆口袋中间 → 切成紧缝 bounce 风暴
- 现象：弯道障碍随机范围首版含 x=46.9（口袋中间）→ 混合场 bounce 404 / 物理 301s
- 根因：障碍摆口袋中间把 5m 转弯区切成两条 ~1.3-2.0m 紧缝，执行层穿缝不利索
- 修复：随机范围收窄到**贴外墙半侧**（右弯 x∈[47.6,48.6]/左弯 [1.4,2.4]，y=5k±1.5），
  两侧走线 ≥1.9m。教训：弯道障碍的几何意义是"逼狗走外侧弯"，不是"把弯道切成迷宫"

### 15.5 门评分加净空权重 —— 门选择混乱卡死（实验否决）
- 现象：pick_gate 加 0.18 净空权重（想让狗优先宽门免边界缝探测）→
  seed7 探索 28% 覆盖卡死未到达（A* 风暴）；seed23 碰撞 238
- 根因：advance/距离/大小的评分结构是无数次实测调出来的平衡，加项即失衡——
  远处宽门系统性赢过近处该确认的前沿门，探索节奏崩溃
- 修复：**回退**。教训：评分公式改动必须多种子验证；单一动机改评分 ≈ 拆承重墙

### 15.6 stall 阈值 8→3 —— 移动障碍挡路被误判假门（实验否决）
- 现象：stall 收紧后 seed7 碰撞 195、seed23 碰撞 14
- 根因：移动障碍临时挡路时狗合法等待（无进展）→ 3 次就拉黑好门 → 狗乱闯遭遇战
- 修复：**回退到 8**。教训：stall 判定分不清"假门"和"被障碍合法挡住"——
  收紧前先想谁会误伤

### 15.7 规划净空 0.3m A/B —— 误差余量误当碰撞余量（实验否决）
- 现象：PASS_CLEAR 0.6→0.3（主人身材账：狗 0.4m 宽+两侧 10cm=0.6m 总宽）→
  纯墙 bounce 2→118、混合场 collision 621
- 根因：规划净空要吸收**感知误差**（0.1m 栅格量化/掠射噪声/弧心偏移 ~0.3m）+
  **控制误差**（DWA 采样点距 0.2m 不插值）+ **遭遇战闭环时间**（5m/s 闭合 + 制动 ~1m），
  不是只留身体宽度。纯墙场没有窄缝 bounce 也爆炸 → 代价在"贴墙行驶"本身
- 修复：回退 0.6m。想压阈值先做小误差（速度外推/0.05m 栅格/插值+窄道限速，见
  gate-width 文档第六节）。教训：**余量分层：碰撞余量在执行层，误差余量在规划层**

### 15.8 先推送后跑测试 —— 流程坑
- 现象：一次性 commit+push 后才发现 test_initial_position 失败（旧契约断言"初始段中央"，
  而改动恰恰是被要求改成段内随机）
- 修复：补改测试再推一次。教训：**测试跑在 commit 之前**，即使只是"小改动"

### 15.9 元教训：多测（换种子/换布局/看录像）是最高产的 bug 探测器
本轮全部 bug 都是"多测一次"钓出来的：换布局 → 豁免擦掉固定障碍（15.3）；
看录像 → 视觉冻结（15.2）+ 探测卡顿；换种子 → 评分实验的随机性依赖（15.5/15.6）。
固定障碍每次不同位置 = 不带 `--seed` 跑（FIXED_SEED 随机），回归/录像用显式种子保复现。

## 十六、08-12 代码审核整改：去真值化 + 定位攻坚战（本轮实测定位）

审核文件：`代码程序审核-2026-08-11.txt`。三处决策层真值使用全部整改（默认运行零特权），
过程暴露出"定位是最大难题"（主人原话：四足里程计 5%/s 误差 + IMU 累积漂移）。

### 16.1 三处真值特权与整改
1. **定位用 `d.qpos` 真值** → 默认 `--odom 1`：带噪里程计（慢变偏差+陀螺漂移模型）
   + scan-to-map 匹配（`simtrack/scan_matching.py`）+ 二维码绝对修正。真值只剩
   物理推进/碰撞统计/评分打印。
2. **DWA 读 `random_field.velocities` 真值速度** → `ObstacleTracker`
   （`simtrack/obstacle_tracker.py`）：障碍命中点聚类+帧间关联+速度 EMA。
3. **经验墙写入用 `sample_hf`/obs_world 真值确认** → `HIT_CONFIRMED`：
   只认激光**直接命中**格（掠射填充/墙厚先验推断不算），防假墙自指扩散。
4. 物理/认知分层：激光投射用真值位姿（物理模拟），写地图用估计位姿——
   点云相对机身精确，放错位置的是狗自己的位姿误差。这才是真实 SLAM 的形态。

### 16.2 陀螺偏置封顶要现实 —— 无上限 yaw 漂移 100s 转飞 🎯
- 现象：里程计 5% 模型首跑全程漂移 7m+，二维码修正后反而更错（20m 外修正漂移 8.3m）
- 根因：陀螺偏置游走 ±1°/s 封顶 → 100s 累积 30°+，超出匹配窗（±4.5°）再也拉不回；
  yaw 错 10° 时 20m 外的二维码反解位姿错 3.5m，修正=投毒
- 修复：偏置封顶 ±0.3°/s（像样 MEMS 的量级）；转弯比例偏差封顶 ±2.5~5%。
  教训：**噪声模型的"累积型参数"必须先算 saturating 量级**——白噪声无关紧要，
  偏置随机游走才是杀手

### 16.3 假终点死锁 —— 远距观测把估计锁到狗脚边，A* 空转 9 万步 🎯
- 现象：纯墙场狗在末通道右端 44m 外看到球，est 被锁到 (49.2,47.6)（狗脚边），
  狗对着墙角重规划 3.2 万次 A*（每 5 步一次）直到超时
- 根因：远距针孔距离是噪声（像素量化/背景板遮挡），中位数/三角定位被垃圾观测打飞；
  且"估计锁定"机制防跳变 → 错了也解不开；到达判定永远不满足（球不在那）→ 无逃生
- 修复：①远距（>25m）观测不更新位置估计（只信方位角）②**直奔死锁保险**：狗贴估计点
  2.5m 内 >4000 步仍判不了到达 → 作废估计回探索，5m 区域拉黑 4 万步 ③**视觉伺服终局**：
  球新鲜可见 ≤25m 且感知视线通畅 → 直接把观测投影设为目标（相对几何精确，全局漂移无关）
- 教训：**"看到 X 却到不了 X"必须有超时作废机制**；终局用直接感知，别过中间表示

### 16.4 滚动局部障碍层 —— 全局地图会骗你，3 秒内的激光不会 🎯🎯（本轮最大根修）
- 现象：固定障碍场 collision **104058**（狗嵌进障碍体半场比赛）；纯墙场卡死弯口
  （d_clear=0.10 但真值是路）；混合场 yaw 漂到 34543°
- 根因：硬栅格地图随累计位姿漂移**自洽地错**（幻影墙/障碍错位）；执行层的移动护卫
  只看地图 → 狗物理开进真墙/真障碍（contype=0 没有物理兜底，防撞全靠算法）
- 修复（ROS rolling obstacle layer / costmap 分层思想）：`LOCAL_STAMP` 滚动层——
  每次扫描把**直接命中格**（墙+障碍无膨胀；障碍面 +0.4m 膨胀）打上时间戳，
  移动护卫/前瞻制动/DWA 全部叠加"最近 3s 内激光直读"。3s 窗口内里程计增量误差
  ~1-2%≈0.1m 级，永远局部准确。距离沿视线方向是精确的（位姿误差只带横向偏移），
  所以正前方的墙/障碍永远刹得住
- 效果：纯墙 5% 到达（0 撞）、特征障碍场 0 撞探图 99.5%、混合场 0 撞
- 教训：**碰撞保护不该依赖全局地图的精度**——它在任何定位质量下都必须成立

### 16.5 局部层零初始化陷阱 —— 全图"新鲜"堵死起点
- 现象：局部层首版上线后狗在起点原地 STOP 风暴（d_clear=0.10，四周明明开阔）
- 根因：`LOCAL_STAMP` 零初始化，新鲜判定 `stamp > scan_step-60` 在开局 `0 > -40`
  恒真 → **全图每个格都是"新鲜障碍"**
- 修复：初始化 -10⁶。教训：**时间戳/版本号类缓存的"空"初值必须落在"永不过期"反面**

### 16.6 风暴隔离（已移除）—— 恢复机制切断了逃生通道
- 尝试：bounce 率过高判"迷失"→ 冻结全局地图写入/匹配/绝对修正（防不可信位姿投毒）
- 实测：v13 在 step 2436 锁死到超时——冻结把**射线清除**也冻了，幻影墙永不被擦掉
  → 狗永远逃不出 → bounce 率降不下 → 永不解除（死地）
- 教训：**恢复机制不能切断自身的逃生通道**（隔离写入=隔离了自我纠正的信息流）

### 16.7 scan-to-scan 帧间匹配（已回退）—— 参照物比算法重要
- 尝试：改全局图匹配为 LOAM 式连续帧匹配（压制增量漂移）
- 实测：v13 全程锁死——细网格（0.05m）+ 全增益把每帧的量化噪声当修正，
  连续应用 = 随机游走放大器（每帧±2cm 的"修正"累积成几十 cm 的错位注入）
- 教训：**相关匹配的最小可分辨修正量是栅格尺寸**；细网格+高增益不会更准，只会更吵

### 16.8 ω 噪声封顶 —— bounce 朝向瞬跳的物理建模
- 现象：bounce 把 yaw 瞬跳 ±π，里程计按 ω=628 rad/s 积分比例噪声 → 每次 bounce
  yaw 误差 5°+，几百次 bounce 后随机游走发散
- 修复：噪声项按 ≤3 rad/s（快速真实转身）封顶，转角本身足额积分。物理依据：
  受控原地转身有陀螺全程积分，误差远小于步态推算；真实陀螺也有量程饱和
- 教训：**仿真里的"瞬跳"便利不能原样进噪声模型**——要想清楚真实传感器在这个
  动作下到底读到什么

### 16.9 终点判定在漂移下的新形态
- 真值距离 vs 估计距离可能差 1-2m（漂移）；到达判定全是相对量（针孔距离+视线+
  面积+新鲜度），不依赖全局坐标 → 仍然可靠。成绩单里 `dist_to_true_finish`
  （评分用真值）与判定逻辑分离，读数要看口径

### 16.10 当前边界（诚实声明）
- 5%/s 噪声 + 稀疏二维码（每通道 1 个）下，**纯墙场**与**特征障碍场**可到达；
  障碍密/机动多的局靠局部层保 0 撞，但全局漂移偶尔让 finish_est 错位 →
  靠死锁保险+视觉伺服兜底。长直道沿程方向匹配退化（ROS 同）——靠二维码/特征障碍/
  弯口端墙。真值级鲁棒要 EKF 融合 + 位姿图回环（下阶段 A2+RL 后做）

### 16.11 狗脚自清区缺失 —— 膨胀戳盖住房主，移动护卫永冻 🎯
- 现象：混合场狗在 (32.7,9.9) 完全冻结数 10 万步：前瞻 4m 畅通（d_fwd=4.0）却一步不动，
  bounce 刷到 1100+
- 根因：障碍在 0.5-0.9m 处时其命中点 +0.4m 膨胀戳**盖住狗当前格** →
  移动护卫 `_local_hit` 在 nx≈bx 处恒真 → speed=0 每步 → 永冻；
  爬出分支又要求地图 blocked（地图没有）→ 进不去
- 修复：打戳跳过狗身 0.35m 自清区（该处由接触区豁免+逃逸逻辑接管）。
  教训：**任何"安全层"都要回答"狗已经站在里面怎么办"**——自检区是 ROS costmap 的
  footprint clearing 的对应物

### 16.12 终点估计被远距帧污染 —— 已收敛的正确估计被拖到 35m 外 🎯
- 现象：特征场覆盖 99.99%、漂移 0.69m、二维码 10/10、球也看见了，却没到达
  （finish_est_err 35.5m）
- 根因：est 在 ch8 已三角收敛到 (2.5,47.2)（真值 (2.5,47.5)，差 0.3m！），
  之后狗绕道进 ch9 时 >25m 的观测帧（球被墙沿/距离部分遮挡，针孔 fd 30-80m）
  的**质心方位角也错了**（看得见的是球的月牙，不是球心）→ 三角定位混入垃圾射线
  把 est 拖走。锁定逻辑防跳变 → 错上加错
- 修复：fd>25m 的帧只记录"看到球"的事实，不进 finish_obs/finish_rays 任何估计
  （方位角只在球完整可见时可信）。教训：**传感器数据的可用性门槛要按物理状态
  （遮挡比例）设，不是能检测就用**

### 16.13 HPA 目标格越界崩溃
- 现象：EXPLORE-DONE 切 KNOWN_MAP 直奔时 `hpa.plan()` IndexError（goal x=50.0 → 格 500）
- 修复：目标格钳制 [0, GRID_N-1]（两处调用点）。教训：估计目标可以贴地图边，
  格换算必须钳制——**边界值测试要覆盖"正好 50.0"这种点**

